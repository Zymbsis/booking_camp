export const selectCamperList = (state) => state.campers.campers;
export const selectFavoritesId = (state) => state.campers.favoritesId;
export const selectHasNextPage = (state) => state.campers.hasNextPage;
export const selectFavoritesList = (state) => state.campers.favoritesList;
export const selectPage = (state) => state.campers.page;
export const selectIsLoading = (state) => state.campers.isLoading;
